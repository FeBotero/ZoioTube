import styled from "styled-components";

export const ContainerCard = styled.div`
  width: 20rem;
  cursor: pointer;
  .videoPreview {
    width: 15rem;
    border-radius: 0.5rem;
  }
  .imgAuthor {
    height: 3rem;
    width: 2.8rem;
    border-radius: 50%;
  }
  .contentAuthor {
    display: flex;
    gap: 1rem;
    padding-top: 1rem;
  }
`;
