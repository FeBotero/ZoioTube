import styled from "styled-components";

export const ContainerCard = styled.div`
  cursor: pointer;
  .videoPreview {
    width: 20rem;
  }
  .imgAuthor {
    width: 3rem;
    border-radius: 50%;
  }
  .contentAuthor {
    display: flex;
    gap: 1rem;
    div {
    }
  }
`;
